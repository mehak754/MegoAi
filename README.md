## Project Structure

```plaintext
MegoAi/
│── app.py              # Flask entry point
│── jarvis_logic.py     # AI logic
│── templates/
│    └── index.html
│── static/
│    └── style.css
│── requirements.txt
│── .env                # (this will not be uploaded, instead use Render dashboard)
│── Procfile            # (new file, needed for Render)
```
